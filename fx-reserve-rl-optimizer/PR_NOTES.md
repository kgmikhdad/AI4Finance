# PR notes

This branch adds the FX reserve RL optimizer project under `fx-reserve-rl-optimizer/`.

Local checks to run:

```bash
cd fx-reserve-rl-optimizer
pip install -r requirements.txt
pytest tests/
streamlit run app/streamlit_app.py
```
