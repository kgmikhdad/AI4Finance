# Agents module

This module contains training and evaluation helpers.

Use:

```bash
python -m src.agents.train_ppo --config configs/rl_config.yaml
```

The Streamlit dashboard should load trained outputs rather than train models during app runtime.
