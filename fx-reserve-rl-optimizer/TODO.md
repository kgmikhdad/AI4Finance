# TODO

- Run tests locally: `pytest tests/`
- Run dashboard locally: `streamlit run app/streamlit_app.py`
- Replace stub `README.md` with `PROJECT_README.md`
- Train PPO with `python -m src.agents.train_ppo --config configs/rl_config.yaml`
- Export PPO evaluation results and add them to dashboard
- Deploy to Streamlit Community Cloud
